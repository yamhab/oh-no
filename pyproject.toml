[project]
name = "oh-no"
version = "0.1.0"
description = "UNO in the terminal"
readme = "README.md"
requires-python = ">=3.14"
license-files = ["LICENSE.md"]

[project.scripts]
oh-no = "oh_no.__main__:main"
